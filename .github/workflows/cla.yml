name: CLA

# STUB — CLA Assistant wiring (see CLA.md). Enable by adding a PERSONAL_ACCESS_TOKEN
# secret and uncommenting the action below. Kept inert so no secrets are required.

on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, synchronize]

jobs:
  cla:
    runs-on: ubuntu-latest
    steps:
      - name: CLA Assistant (stub)
        run: echo "CLA Assistant stub — configure contributor-assistant/github-action and PERSONAL_ACCESS_TOKEN to enable."
        # - uses: contributor-assistant/github-action@v2
        #   env:
        #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        #     PERSONAL_ACCESS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        #   with:
        #     path-to-signatures: signatures/version1/cla.json
        #     path-to-document: https://github.com/OWNER/REPO/blob/main/CLA.md
        #     branch: main
