# syntax=docker/dockerfile:1
# Dockerfile.dashboard — STUB (implemented in Phase 8).
# Multi-stage pnpm build of @heed/dashboard will land here.
FROM node:22-slim
WORKDIR /app
CMD ["node", "-e", "console.log('heed dashboard image is a Phase 0 stub')"]
