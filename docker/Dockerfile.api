# syntax=docker/dockerfile:1
# Dockerfile.api — STUB (implemented in Phase 8).
# Multi-stage pnpm build of @heed/api will land here.
FROM node:22-slim
WORKDIR /app
CMD ["node", "-e", "console.log('heed api image is a Phase 0 stub')"]
