# syntax=docker/dockerfile:1
# Heed background worker (BullMQ): AI tasks, webhook delivery, email.
FROM node:22-slim AS base
RUN corepack enable
WORKDIR /app

COPY . .
RUN pnpm install --frozen-lockfile

ENV NODE_ENV=production
CMD ["pnpm", "--filter", "@heed/worker", "start"]
